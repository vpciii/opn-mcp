# Architecture Decision Records

Numbered, append-only records of significant decisions for this
project. See `$METHODOLOGY_HOME/methodology.md` §1 for when an ADR is
required, and `$METHODOLOGY_HOME/templates/adr/_template.md` for the
format.

Newest first when browsing. Never edit an accepted ADR — supersede it
with a new one and mark the old one `Superseded by NNNN`.
